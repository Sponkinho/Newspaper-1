<div class="row">
    <p class="d-flex justify-content-around">

        {% for category in categories %}
            <span>
                <a href="#" class="text-decoration-none text-muted item-hover">{{ category.name }}</a>
            </span>
        {% endfor %}

    </p>
</div>